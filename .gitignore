__pycache__/
*.py[cod]
*.pkl
*.csv
*.joblib

.venv/
.python-version

# GCP service account keys
*-service-account*.json
*gcp*.json
*credentials*.json
google_credentials.json
service-account.json
key/
keys/

# Environment files
.env
.env.*
gha-creds-*.json
!.env.example

# OS
.DS_Store
Thumbs.db

# GCP credential files
.gcp-creds.json

# Generated data artifacts
artifacts/
# 로컬 준비 모델/캐시 경로를 포함하는 Harness 실행 설정
/harness_config.json
data/generated/
asset/
/tmp/

# 로컬 MLflow 추적 저장소 (MLFLOW_TRACKING_URI=file:./mlruns)
mlruns/

# Large raw datasets (Kaggle backfill 원본/샘플, persona 원본 등)
*.zip
data/raw/youtube/
data/raw/personas/
Nemotron-Personas-Korea/

# Codex 세션 스크래치(임베디드 repo 포함)
.codex-tmp/

# 세션 스크래치 작업 공간
scratchpad/

# 에이전트 생성 HTML 리포트(docs 최상위만 — docs/archive/reports/는 추적 유지)
/docs/*.html

# 에이전트 도구 세션 스크래치
.omo/
.worktrees/
.claude/worktrees/

# 로컬 생성 산출물(PDF 등)
output/

# Airflow DAG는 인접 저장소 SKYAHO/Autoresearch-airflow 소유다.
# 최상위 dags/ 는 #142 레거시 제거 후 되살아난 로컬 잔재이므로 추적하지 않는다 (#754)
/dags/

# 에이전트 도구 세션 스크래치 (#754)
.gjc/
.playwright-mcp/
.superpowers/

# 워크벤치 확인용 스크린샷 등 최상위 이미지 (#754)
/*.png

# 워크스페이스 전용 로컬 메모 — 파일 스스로 "Git에 커밋하지 않는다"고 선언한다 (#754)
/agent.md
